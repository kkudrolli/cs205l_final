

# Wikipedia Corpus


Go to `https://dumps.wikimedia.org/enwiki/latest/`
Download `enwiki-latest-pages-articles.xml.bz2`

# Sem Cor Corpus
```
pip install nltk
python
nltk.download()
```
Then select `semcor` under `corpora`.
